const cardBtn = document.querySelector('#cardBtn');

console.dir('hello');

cardBtn.addEventListener('click', () => {
    // body.textDiv1. = cardColors[Math.floor(Math.random() * colors.length)];
});
